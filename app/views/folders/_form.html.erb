<h2>Create a new folder</h2>
<%= form_for @folder, :url => {:controller => 'folders', :action => 'create'} do |f| %>

	<%= f.text_field :name, placeholder: 'Name' %><br/>
	<%= f.text_area :description, placeholder: "Description..." %>
	<%= f.submit "Create Folder" %>	
<% end %>